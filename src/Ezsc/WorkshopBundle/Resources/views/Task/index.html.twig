{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Task list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Name</th>
                <th>Datecreatedat</th>
                <th>Datedeadlineat</th>
                <th>Priority</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('practice_task_show', { 'id': entity.id }) }}">{{ entity.name }}</a></td>
                <td>{% if entity.dateCreatedAt %}{{ entity.dateCreatedAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if entity.dateDeadlineAt %}{{ entity.dateDeadlineAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.priority }}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('practice_task_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('practice_task_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('practice_task_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
